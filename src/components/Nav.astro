---
import styles from '../styles/Nav.module.css'
const pathname = Astro.url.pathname
---

<nav>
  <ul class={styles.navList}>
    <li>
      <a
        href="/"
        class={`${styles.navLink} ${pathname === '/' ? styles.active : ''}`}
      >
        home
      </a>
    </li>
    <li>
      <a
        href="/blog"
        class={`${styles.navLink} ${pathname.startsWith('/blog') ? styles.active : ''}`}
      >
        blog
      </a>
    </li>
  </ul>
</nav>
